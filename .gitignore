**/.claude/settings.local.json

# Local installer/test state and Python bytecode (never committed).
/.state/
/.cache/
__pycache__/
*.py[cod]

# Supplied only by the separate private dotfiles-work checkout.
/config/shell/work.sh
